# Enriquecedor de Leads
# Opcional: sem a chave a ferramenta funciona, só não gera a leitura comercial.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash

# Opcional: usa fixtures locais em vez de chamar as APIs (desenvolvimento offline).
# LEADS_MOCK=1
