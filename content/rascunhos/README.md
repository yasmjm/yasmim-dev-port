# Rascunhos

Posts que ainda não estão prontos. Nada nesta pasta é lido pelo site —
`lib/posts.ts` só olha para `content/posts/`.

Para publicar um deles: mova o arquivo para `content/posts/` e troque
`draft: true` por `draft: false` no frontmatter.
